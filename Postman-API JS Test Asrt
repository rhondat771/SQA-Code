# Common SQL commands

//create inner join from tables- employees:id, name, department_id & departments:id, dept_name  

SELECT 
    employees.name,
    departments.dept_name
FROM 
    employees
JOIN 
    departments
ON 
    employees.department_id = departments.id;
